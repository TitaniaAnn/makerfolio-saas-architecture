{
    "name": "titaniaann/makerfolio-saas-architecture",
    "description": "Reference cut of the multi-tenant schema-per-tenant architecture behind makerfolio: search_path tenancy, idempotent per-tenant migrations, the INSERT-first webhook contract, transitionTo state machines, and the /caddy-ask cert gate — in a generic notes toy domain.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5 || ^11.0"
    },
    "autoload": {
        "psr-4": {
            "MakerfolioArch\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "sort-packages": true
    }
}
